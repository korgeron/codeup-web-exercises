/*"use strict";

console.log("Hello from external JavaScript");
//JS with HTML Exercises
/*
// PROBLEM 1
alert('Welcome to my Website!');
// PROBLEM 2
let color = prompt("What is your favorite color?");

alert(color + " is my favorite color too.");



 */
/*
//PROBLEM 3 STILL WORKING ON
let question = confirm("Would you like to rent a movie for $3 per day?");
    if (question >= 1) {
    } else {
       onclose(alert("Maybe next time."))
    }
let movie1 = "The little Mermaid";
let movie2 = "Brother Bear";
let movie3 = "Hercules";
let choices = ("Your Choices Are: " + movie1 + ", " + movie2 + ", " + movie3);
    if (confirm(choices) >= 1) {
  } else {
        onclose(alert("Bad selections? OOPSE!!"))
    }
function total1() {
    let totalDays1 = parseInt(prompt("Enter number of days to rent: "))
    return (totalDays1 * 3)
}
let choice1 = "Would you like to rent " + movie1;
/*function total1() {
  let totalDays1 = parseInt(prompt("Enter number of days to rent: "))
    return (totalDays1 * 3)
}
 */
 /*   if (confirm(choice1) >= 1) {
        total1()
    } else {
        total1 = 0;
    }
function total2() {
    let totalDays2 = parseInt(prompt("Enter number of days to rent: "))
    return (totalDays2 * 3)
}
let choice2 = "Would you like to rent " + movie2;
/*function total2() {
    let totalDays2 = parseInt(prompt("Enter number of days to rent: "))
    return (totalDays2 * 3)
}
 */
 /*   if (confirm(choice2) >= 1){
        total2()
    } else {
        total2 = 0;
    }
function total3() {
    let totalDays3 = parseInt(prompt("Enter number of days to rent: "))
    return (totalDays3 * 3)
}
let choice3 = "Would you like to rent " + movie3;
/*function total3() {
    let totalDays3 = parseInt(prompt("Enter number of days to rent: "))
    return (totalDays3 * 3)
}
 */
  /*  if (confirm(choice3) >= 1) {
        total3()
    } else {
        total3 = 0;
    }
    alert(total1() + total2() + total3());

*/

